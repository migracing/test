# Test Project for GitHub Actions Docker Build

This is a simple Python test project that prints "Hello World" to a log file inside a Docker container.  
It demonstrates building a Docker image with GitHub Actions and pushing it to GitHub Container Registry (GHCR).
